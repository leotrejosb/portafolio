/** Shared Tailwind class strings for repeated layouts */
export const panel =
  "w-screen shrink-0 h-full snap-start overflow-y-auto overflow-x-hidden px-[clamp(24px,5vw,72px)] pt-[calc(var(--hdr,130px)+clamp(24px,4vh,48px))] pb-[clamp(24px,5vw,72px)]";

export const panelCentered = `${panel} flex items-center`;
